name: Update Axiom Brain

on:
  workflow_dispatch: # يسمح لك بتشغيله يدوياً
  push:
    branches:
      - main
    paths:
      - 'packages/**'

jobs:
  ingest-knowledge:
    runs-on: ubuntu-latest
    
    # --- الإصلاح 1: تحديد مجلد العمل ---
    defaults:
      run:
        working-directory: ./packages/bots/axiom-assist-bot

    steps:
      - name: Checkout Monorepo Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Force Clean Install (Fix Lockfile)
        # --- الإصلاح 2: حذف القفل التالف أولاً ---
        run: |
          rm -f package-lock.json
          npm install

      - name: Run Ingestion Script (Feed the Brain)
        # --- الإصلاح 3: تحديد المسار الجذري للمشروع ---
        run: node ingest.mjs
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PINECONE_API_KEY: ${{ secrets.PINECONE_API_KEY }}
          PINECONE_INDEX_NAME: ${{ secrets.PINECONE_INDEX_NAME }}
          PROJECT_PATH: ${{ github.workspace }} # المسار الجذري للمستودع
```eof

8.  بعد اللصق، انزل للأسفل واضغط على الزر الأخضر **`Commit changes...`**.

---

### 🚀 خطوتك التالية (المحاولة الرابعة والأخيرة)

1.  بعد أن تضغط "Commit changes"، انتظر 10 ثوانٍ.
2.  اذهب إلى تبويب **`Actions`** مرة أخرى.
3.  اختر `Update Axiom Brain` من القائمة.
4.  اضغط **`Run workflow`** يدوياً مرة أخرى.

هذه المرة، أنا أضمن لك أن خطوة `Force Clean Install` ستعمل، وستقوم بحذف الملف التالف، وسيتم تثبيت `flat`، وستنجح "تغذية الدماغ" (✅). نحن على خط النهاية!
